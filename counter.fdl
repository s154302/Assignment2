dp adder(	in a : tc(32);
			in b : tc(32);
			out c : tc(32)) {

	reg sum : tc(32);

	sfg add {
	//	$display($dec, "a: ", a);
	//	$display($dec, "b: ", b);
	//	$display($dec, "c: ", c);

		sum = a + b;
		c = sum;
	}

}

system counterSystem {
	counter(c);
}